{
  "name": "exposure-game",
  "version": "0.3.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "npx serve .",
    "dev": "npx serve .",
    "check": "node --check app.js && node --check book-engine.js && node --check living-time-patch.js && node --check living-social-engine.js && node --check ai-bridge.js && node --check api/_security.js && node --check api/health.js && node --check api/talk.js && node --check api/director.js && node --check api/killer.js"
  },
  "dependencies": {
    "openai": "latest"
  },
  "devDependencies": {
    "serve": "latest"
  }
}
